/**
 * Nubeware Configuration
 * Centralized settings for the application
 */

const CONFIG = {
    // API Base URL for RAG and AI services
    // En producción, esta URL se gestionaría mediante variables de entorno en el servidor
    API_BASE: 'https://api.nubeware.ai/api'
};
